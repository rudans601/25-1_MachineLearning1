# 2주차 R 기초

## 이번 주 핵심

이 주차에서는 복잡한 통계 분석보다 `R` 문법에 익숙해지는 것이 가장 중요한 목표였습니다. 숫자를 직접 계산하고 변수에 저장하는 방법, 배열을 만들고 원하는 위치의 값을 꺼내는 방법을 연습하면서 이후 통계 코드의 기초가 되는 표현을 익혔습니다.

## 코드 설명

아래 코드는 평균 계산, 일부 값만 사용한 평균 계산, 가중합 계산, 간단한 수식 처리, 그리고 `array` 생성과 인덱싱을 연습한 내용입니다. 아직 통계 검정 단계는 아니지만, 데이터를 계산하고 다루는 기본 감각을 만드는 출발점 역할을 했습니다.

## 원본 R 파일

- [`R/ml_2-1.R`](../../R/ml_2-1.R)

## 코드

```r
#1
a <- (75 + 88 + 91 + 68 + 82) / 5
b <- (60 + 87 + 55 + 47 + 92) / 5
#2
c <- (75 + 88 + 82) / 3
d <- (60 + 87 + 55) / 3

#3
one = 75 * 0.5 + 60 * 0.3
two = 88 * 0.5 + 87 * 0.3
three = 91 * 0.5 + 55 * 0.3
four = 68 * 0.5 + 47 * 0.3
five = 82 * 0.5 + 92 * 0.3

print("two,five,three,one,four")

#4
zeroy = sqrt((5.5/5)* (7/5.5) * (10/7),3) -1

print(zeroy)

t=array(data = c(1, 2, 3, 4, 5, 6), dim = c(2, 3))
t[1:4]
t[1,1]
```
